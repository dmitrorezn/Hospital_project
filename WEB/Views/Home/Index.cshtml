@using BLL.Entities
@model Web.Models.VisitsViewModel
@{
    ViewBag.Title = "Home Page - All visits";
}

<h2>List of all visits:</h2>

<table class="table">
    <tr>
        <td>
            N
        </td>
        <td>
            Patient`s name
        </td>
        <td>
            Patient`s surname
        </td>
        <td>
            Doctor`s name
        </td>
        <td>
            Doctor`s surname
        </td>
        <td>
            Visit date and time
        </td>
    </tr>
    @{
        int i = 0;

    }
    @foreach (var item in Model.DocsPatientsDTO)
    {
        <tr>
            <td>
                @item.VisitId
            </td>
            <td>
                @item.PatName
            </td>
            <td>
                @item.PatSurname
            </td>
            <td>
                @item.DocName
            </td>
            <td>
                @item.DocSurname
            </td>
            <td>
                @item.VisitTime
            </td>
        </tr>
        i++;
    }
</table>

@Html.ActionLink("Plan a visit", "Index", "Doctor")